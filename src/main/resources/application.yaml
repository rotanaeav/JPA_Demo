spring:
  jpa:
    hibernate:
      ddl-auto: update #create alter drop..
    show-sql: true
    properties:
        hibernate:
          format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: postgres
    password: 9903
  application:
    name: SB-MVC

